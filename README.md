# WiDS_Heatwave
	The first branch contains code to convert raw ERA 5 data to processed data in csv format, ready to be used in excel.	
	The second branch contains code of Regression model used both for univariate and multivariate datasets.
	The third branch contains code to generate a pearson corelation matrix from the selected datasets.
